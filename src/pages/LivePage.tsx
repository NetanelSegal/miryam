import { Heading, Container } from '@/components/ui'

export function LivePage() {
  return (
    <Container size="full" className="min-h-screen flex items-center justify-center">
      <Heading level={1} gradient>Live Display</Heading>
    </Container>
  )
}
